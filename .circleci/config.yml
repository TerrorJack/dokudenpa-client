version: 2
jobs:
  build:
    docker:
      - image: circleci/node:9-stretch
    steps:
      - checkout
      - run: |
          npm install -g parcel-bundler
          npm install
          parcel build
